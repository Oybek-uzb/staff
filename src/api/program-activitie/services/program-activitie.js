'use strict';

/**
 * program-activitie service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::program-activitie.program-activitie');
